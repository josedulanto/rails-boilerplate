.ui.one.column.centered.grid
  .sixteen.wide.mobile.seven.wide.tablet.six.wide.computer.center.aligned.column
    %h2.ui.top.attached.left.aligned.header Forgot your password?
    .ui.attached.segment
      = simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'large with-message' }) do |f|
        = f.error_notification
        = f.input :email, required: true, autofocus: true, label: false, placeholder: "Email"
        = f.button :submit, "Send me reset password instructions", class: "large fluid"
    
    = render "devise/shared/links"